import './login.scss';

interface LoginProps {
    // add your props here
}

export const Login = ({}: LoginProps) => {
    return (
        <div className="login">
            <h2>Login works!</h2>
        </div>
    );
};
