import './button.scss';

interface ButtonProps {
    // Agregá tus props acá
}

export const Button = ({}: ButtonProps) => {
    return (
        <div className="button">
            <h2>Button works!</h2>
        </div>
    );
};
